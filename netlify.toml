[build]
  command = "(npx tinacms build || echo '[WARN] TinaCMS build failed — /cms admin will be unavailable until TinaCloud syncs') && npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
